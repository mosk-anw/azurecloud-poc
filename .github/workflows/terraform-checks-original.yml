
name: Terraform Best Practices Checks

on:
  pull_request:
    branches:
      - main

jobs:
  terraform_checks:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Setup Terraform
      uses: hashicorp/setup-terraform@v3
      with:
        terraform_version: 1.x.x # You can specify a precise version like 1.5.7

    - name: Terraform Init
      id: init
      run: terraform init

    - name: Terraform Validate
      id: validate
      run: terraform validate -no-color

    - name: Terraform Format Check
      id: fmt
      run: terraform fmt -check=true
